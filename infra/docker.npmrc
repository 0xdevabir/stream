# Used only inside Docker image builds (copied as /.npmrc before pnpm install).
# Slow or flaky registry.npmjs.org links otherwise abort mid-download.
fetch-timeout=600000
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
network-concurrency=4
